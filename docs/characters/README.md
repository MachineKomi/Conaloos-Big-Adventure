# Character docs

One markdown file per character sprite. Use the template in `SPEC.md §6.2`. Cross-character remixes go in `remixes.md`.

A character bio must exist before that character speaks a single line. Voice and obsession come from the bio; the bio drives the rhymes, not the other way around.
