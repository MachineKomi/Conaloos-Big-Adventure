# Scene docs

One markdown file per `bg_*` background. Use the template in `SPEC.md §6.1`.

A scene doc must exist before a hand-authored scene definition lands in `/src/content/scenes.js`. If you add a new scene, write the doc *first*, then the code — the doc is where you reason through the design, the code is where you record the result.
